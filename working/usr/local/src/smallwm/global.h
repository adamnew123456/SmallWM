#ifndef __GLOBAL__
#define __GLOBAL__

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

#include <X11/Xlib.h>
#include <X11/keysym.h>

#define MAX(a,b) ((a) > (b) ? (a) : (b))
#define SHELL "xterm"

#endif
